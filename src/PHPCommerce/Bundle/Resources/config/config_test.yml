imports:
    - { resource: config_dev.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false
use pdo_sqlite database
parameters:
    database_driver: pdo_sqlite
    database_path: %kernel.cache_dir%/test.db

